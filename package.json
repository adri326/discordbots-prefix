{
  "name": "discordbots-prefix",
  "version": "1.0.0",
  "description": "Checks if a prefix isn't already used",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "request": "^2.85.0",
    "request-promise-any": "^1.0.5"
  }
}
